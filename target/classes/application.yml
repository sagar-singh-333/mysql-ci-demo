spring:
  datasource:
    url: jdbc:mysql://localhost:3306/testdb
    username: root
    password: Root@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: always
